统一测试准备
演示前先完成：
运行 morrow robot doctor，确认飞书认证、日历权限和用户搜索均正常。
确认 Morrow /status、Server /health 正常，小派在线。
准备两个明确且可搜索的飞书测试账号，避免同名。
确认 Server、Morrow、串口日志时间均为 Asia/Shanghai。
所有日程标题加 [DEMO]，演示后方便删除。
同时录制小派画面和声音，保留 Server/Morrow 日志。
能力	主验收点	建议耗时	当前状态
创建日历并邀请两人	日程字段、两位邀请人、飞书通知	5分钟	可测
下午外出主动提醒	定时扫描、notice链路、只提醒一次	5–10分钟	可测
讲笑话	语音识别、指令匹配、TTS播放	2分钟	可测
每30分钟久坐提醒	周期调度、重复触发、冲突处理	需加速测试	当前未完成

1. 创建飞书日历并邀请两个人
建议直接对小派说：
小派同学，帮我创建明天下午三点到三点半的“[DEMO] 演示联调”日程，邀请张三和李四参加。

把张三、李四替换成两个真实测试账号。
验收步骤：
观察小派是否正确识别时间、标题和两位参会人。

Morrow 应先分别搜索两位用户并解析成 open_id，然后调用日历创建。

打开飞书日历，检查：
日期为明天。
时间为 15:00–15:30。
标题完全正确。
被邀请人恰好是指定的两个人；组织者不计入“两位邀请人”。
两个测试账号都收到邀请。

小派应播报创建成功；如果 Morrow 返回 <happy>，开心表情只能在音频开始时出现，播放结束后恢复 calm。

建议补一个异常测试：
帮我创建明天下午三点的演示会议，邀请小王参加。

如果存在多个“小王”，Morrow 应要求澄清，不能擅自邀请错误用户，也不能先创建一个缺少参会人的日程。
通过标准：日程内容正确、两位邀请人正确、两人均收到邀请、没有重复日程。
2. 主动提醒下午有外出
当前外勤识别依赖标题或地点包含：
外勤
客户
拜访
现场
调研
生产配置默认提前60分钟提醒，Morrow 每60秒扫描一次，并允许约±1分钟的触发窗口。
明天演示时的快速方案
在演示开始前创建一个大约63分钟后开始的日程，例如：
标题：[DEMO] 外勤客户拜访
地点：深圳市民中心
开始时间：当前时间后约63分钟
结束时间：开始后30分钟
这样无需修改生产配置，通常等待1–3分钟即可进入提前60分钟的触发窗口。
验收链路：
Morrow 日志出现外勤日程扫描和 fieldwork_reminder。

Server 日志依次出现：
收到 Morrow robot_notice
主动提醒入队
小派 ACK：received → running → rendered

小派主动播报类似：
你待会有一个外勤日程，主题是……建议提前出发并检查随身物品。


如果天气或路线服务配置正常，提醒中还应包含路程和天气；这两项失败时，基础外勤提醒仍应正常播放。

等待下一次扫描，确认同一日程没有再次提醒。

检查数据库中的最终状态：

SELECT notice_id, kind, state, last_message
FROM morrow_notices
ORDER BY received_at DESC
LIMIT 10;
期望 kind=fieldwork_reminder、最终 state=rendered。
注意：当前主动提醒正文没有表情标签，因此预期是 calm，不要把“必须显示 thinking/happy”作为此项标准。
3. 让小派讲个笑话
当前精确指令有本地确定性路径，以下说法都能命中：
讲个笑话
说个笑话
来个笑话
讲笑话
主测试建议说：
小派同学，讲个笑话。

当前预期内容是固定的“小明因为学校前方请慢行而迟到”的笑话。这条路径适合演示，因为不依赖模型临时生成，稳定性更高。
验收点：
ASR 文本包含“讲个笑话”。
只生成一条 speak 指令，不能重复播放。
使用预缓存或 TTS 成功播放完整笑话。
说话前保持 calm；第一块音频入队后进入回复显示阶段；播放结束200ms后恢复 calm。
播放期间再次说话或发送 Stop，应立即终止且恢复 calm。
还可以增加一个 Morrow 生成路径：
给我讲一个关于程序员的、没听过的冷笑话。

这句话不属于固定别名，可验证 Morrow 自由回复、标签解析、流式分段和表达状态机，但稳定性低于固定笑话，建议作为加分测试，不作为主演示。
4. 固定30分钟触发一次久坐提醒
这一项当前存在明确阻塞：
久坐检测服务启动被注释：[main_command_services.inc (line 801)](/home/myendless/xiaopai/stack-chan/main/main_command_services.inc:801)
当前调试参数是每分钟检测一次、连续检测到用户5次才提醒：[main_app_state.inc (line 75)](/home/myendless/xiaopai/stack-chan/main/main_app_state.inc:75)
因此现有语义是“根据摄像头连续发现用户触发”，不是“固定30分钟触发”。
明天不建议把手动播放久坐音频包装成周期能力。最多可以演示“久坐提醒语音素材能够正常播放”，但应明确调度尚未完成。
真正实现后，建议这样验收：
单元测试虚拟时间：
29:59 不触发。
30:00 触发一次。
59:59 仍只有一次。
60:00 触发第二次。
时间跳跃后不得集中补播多条。

演示加速测试：
临时把30分钟改成30秒。
连续运行100秒。
应在约30、60、90秒各提醒一次，累计正好3次。
测试后恢复30分钟配置。

冲突测试：
到期时小派正在说话：提醒进入 FIFO，等当前回复结束后播放，不能混音。
到期时正在录音：不能打断用户讲话。
Stop/Sleep：取消当前提醒并清理表情。
Server暂时断开：明确选择“跳过本轮”还是“恢复后补一次”，不能连续补播。
重启后：明确周期从重启重新计时，还是保留上次提醒时间。

长时间验收：
连续运行至少65分钟。
应收到两次提醒。
两次间隔为30分钟左右。
不因摄像头是否识别到用户而漂移；如果产品仍要求“检测到用户才提醒”，就不能再称为“固定30分钟”。

明天演示优先级建议是：先演示创建日程，再用提前布置的外勤日程触发主动提醒，然后演示固定笑话；久坐提醒明确标记为待完成项，避免现场等待30分钟后才发现任务实际上没有启动。